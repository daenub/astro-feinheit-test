---
const { content } = Astro.props;
---

<figure class="quote">
    <blockquote class="quote__content" set:html={content.quotation}>
    </blockquote>
    <figcaption>
        <cite class="quote__cite">{content.name} – {content.function}</cite>
    </figcaption>
</figure>

<style>
.quote {
    background-color: var(--oc-gray-2);
    padding: 2rem;
    border-radius: .25rem;

    margin: 0 -2rem 2rem;
}

.quote__content {
    margin-bottom: .5rem;
}

.quote__content :global(p)::before {
    content: "«";
}

.quote__content :global(p)::after {
    content: "»";
}

.quote__cite {
    font-weight: bold;
}
</style>